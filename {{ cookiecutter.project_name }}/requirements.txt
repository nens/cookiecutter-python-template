# Install ourselves, including test dependencies.
-e .[test]

# If your python code uses requests, add "requests" to `[project] -> dependencies`
# in `pyproject.toml` instead of adding it here. You can use this file for some
# development tools or for explicitly pinning dependencies.
